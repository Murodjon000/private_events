<ul class="nav navbar-nav navbar-right collapse navbar-collapse" id="bs-example-navbar-collapse-1"">
  <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown"> 
       Accepted: <%= User.find(@event.enrollments.accepted.map {|en_user| en_user.user_id }).count %> <b class="caret"></b>
      </a>
      <% if User.find(@event.enrollments.accepted.map {|en_user| en_user.user_id }).any? %>
      <ul class="dropdown-menu">
      <% User.find(@event.enrollments.accepted.map { |en_user| en_user.user_id }).each do |user| %>
        <li>
          <%= user.name %>
        </li>
      <% end %>
      </ul>
      <% end %>
  </li>
</ul>